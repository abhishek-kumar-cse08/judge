#include<stdio.h>
#include<stdlib.h>

int main(){
	int i, N, M;
	scanf( "%d", &N );
	printf( "%d\n", N ); 
	for( i=0; i<N; i++ ){
		scanf( "%d", &M );
		printf( "%d\n", M );
	}
	while( 1 ){
		i++;
	}
	return 0;
}
